$('#news_feed').append('<%= j render @feeds %>');
<% if @posts.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @feeds %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>
